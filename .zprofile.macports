# ------------------------------------------------------------------------
# PATH – first two directories are for MacPorts.
export PATH="/opt/local/bin:/opt/local/sbin:$HOME/go/bin:$HOME/.local/bin:$HOME/.pyenv/bin:/Applications/MacVim.app/Contents/bin:$PATH"
# ------------------------------------------------------------------------
# Activate pyenv.
eval "$(pyenv init -)"
# ------------------------------------------------------------------------
# Java settings
#export JAVA_HOME=$(/usr/libexec/java_home -v <SET-HERE>)
# ------------------------------------------------------------------------
# Groovy settings
#export GROOVY_HOME=$HOME/.local/lib/groovy-<SET-HERE>
# ------------------------------------------------------------------------
# Activate fnm - doesn't cause a slow down as nvm.
eval "$(fnm env --use-on-cd --shell zsh)"
# ------------------------------------------------------------------------